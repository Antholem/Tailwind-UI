import React from 'react';
// import theme from '@/app/theme';

const Content = () => {
  // const { text } = theme();

  return (
    <>
      {/* <button className={text.blue}>
        BUTTON
      </button> */}
      <h1>
        TEXT COLOR
      </h1>
    </>
  );
};

export default Content;
